file: "packages/ops/src/registry.ts"
doc: "Central registry of archeglyph operations. Now includes validateOp."

constants: [
  { name: "REGISTRY"
    change_type: MODIFIED
    doc:
      "Brownfield MODIFIED: append validateOp to the array. Add the import "
      "after the existing renderOp import; cast via "
      "`validateOp as unknown as Operation<unknown, unknown>` — same erasure "
      "pattern as the renderOp entry on the line above (Operation's contravariant "
      "param position blocks the direct assignment)."
    type: { unsupported: { reason: UNION_AT_TYPE_POSITION } }
    uses: ["validateOp"]
  }
]
