name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.10

      - uses: bufbuild/buf-setup-action@v1

      # buf.gen.yaml runs `bunx protoc-gen-es`, which resolves the pinned
      # version from node_modules; without it bunx fetches its own.
      - run: bun install --frozen-lockfile

      - name: licence headers
        run: bun run spdx

      - name: typecheck
        run: bun run typecheck

      - name: tests
        run: bun run test

      - name: editor build
        run: bun run build:editor

      - run: buf lint

      # Committed generated TypeScript can drift from the .proto files.
      - name: generated code is current
        run: |
          buf generate
          git diff --exit-code -- packages/proto/src/gen \
            || { echo "::error::packages/proto/src/gen is stale -- run \`bun run proto:gen\`"; exit 1; }
