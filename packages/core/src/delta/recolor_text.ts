// @generated by archebuild — do not edit. Regenerated each build.

import { create } from '@bufbuild/protobuf';
import { ChangeType } from '../../../proto/src/gen/content_pb';
import { Typography, TypographySchema } from '../../../proto/src/gen/style_pb';
import { DiffRoles } from './diff_roles';
import { diffColorFor } from './diff_color';

export function recolorTypography(typography: Typography, change: ChangeType, diffRoles?: DiffRoles): Typography {
  const next = create(TypographySchema, { ...typography });
  if (typography.color !== undefined) {
    next.color = { ...typography.color, value: diffColorFor(typography.color.value, change, diffRoles) };
  }
  if (typography.background !== undefined) {
    next.background = {
      ...typography.background,
      value: diffColorFor(typography.background.value, change, diffRoles),
    };
  }
  return next;
}
