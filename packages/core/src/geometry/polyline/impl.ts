// @generated by archebuild — do not edit. Regenerated each build.

export function distanceToSegment(p: Vec2, a: Vec2, b: Vec2): number {
  const dx: number = b.x - a.x;
  const dy: number = b.y - a.y;
  const lengthSquared: number = dx * dx + dy * dy;

  if (lengthSquared === 0) {
    return Math.hypot(p.x - a.x, p.y - a.y);
  }

  const projection: number =
    ((p.x - a.x) * dx + (p.y - a.y) * dy) / lengthSquared;
  const t: number = Math.max(0, Math.min(1, projection));
  const closestX: number = a.x + t * dx;
  const closestY: number = a.y + t * dy;

  return Math.hypot(p.x - closestX, p.y - closestY);
}
export function distanceToPolyline(p: Vec2, points: Vec2[]): number {
  throw new Error('not implemented');
}
