// @generated by archebuild — do not edit. Regenerated each build.

import { LaidOutDiagram } from '../laid_out_diagram';
import { LayoutError } from '../layout_error';
import { ResolvedDiagram } from '../resolved_diagram';
import { Result } from '@archeglyph/proto/util/result';

export interface LayoutAdapter {
  runLayout(diagram: ResolvedDiagram): Promise<Result<LaidOutDiagram, LayoutError>>;
}
import { LaidOutDiagram } from '../laid_out_diagram';
import { LayoutAdapter } from './';
import { LayoutError } from '../layout_error';
import { ResolvedDiagram } from '../resolved_diagram';
import { Result } from '@archeglyph/proto/util/result';

export class ElkAdapterImpl implements LayoutAdapter {
  runLayout(diagram: ResolvedDiagram): Promise<Result<LaidOutDiagram, LayoutError>> { throw new Error('not implemented'); }
}
