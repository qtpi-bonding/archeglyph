// Generated by `archegraph testgen` — do not edit by hand.
// Re-run `archegraph testgen --spec <name>` to regenerate.

import { describe, expect, test } from 'bun:test';
import { create } from '@bufbuild/protobuf';
// Repaired by hand: the generated body used these schema descriptors
// without importing them. archetest.yaml now carries them in the core header.
import { DeltaSchema, DiagramSchema } from '@archeglyph/proto/gen/content_pb';
import * as fc from 'fast-check';

import { renderDeltaPipeline } from '../src/pipeline/delta_render_pipeline';

describe('testgen_pipeline__renderDeltaPipeline', () => {
    test.skip('ordinary_successful_render', () => {
        // test generation failed: generated body does not reference the bound value `value`
    });

    test.skip('empty_delta', () => {
        // test generation failed: generated body does not reference the bound value `value`
    });

    // WHEN: The stylesheet argument is undefined; stage 2 uses a newly created empty Stylesheet with schemaVersion 1 before resolving and rendering.
    // THEN: Creates an empty schemaVersion-1 Stylesheet for stage 2, then resolves, lays out, and renders successfully.
    test('undefined_stylesheet', () => {

    });

    test.skip('stylesheet_with_default_theme_binding', () => {
        // test generation failed: generated body does not reference the bound value `value`
    });

    // WHEN: The stylesheet binds no themes; resolveThemes supplies the default binding using the documented fallback [['default', 'dark']], which is then used for default component and diff styling.
    // THEN: Uses the resolveThemes fallback default binding of [['default', 'dark']] for default components and diff styling.
    test('stylesheet_without_theme_bindings', () => {

    });

    test.skip('multiple_theme_bindings', () => {
        // test generation failed: generated body does not reference the bound value `value`
    });

    test.skip('merged_deleted_element_with_component_kind', () => {
        // test generation failed: generated body does not reference the bound value `value`
    });

    test.skip('resolve_pipeline_failure', () => {
        // test generation failed: generated body does not reference the bound value `value`
    });

    test('layout_failure', () => {
        fc.assert(
            fc.property(fc.constantFrom('layout-failure'), (value) => {
        void value;
        const target = create(DiagramSchema, { id: 'target' });
        const delta = create(DeltaSchema, {});
        const layoutEngine = {
          layout: async () => ({ kind: 'err' as const, error: { message: 'layout failed' } }),
        };
        return renderDeltaPipeline(target, undefined, new Map(), layoutEngine, delta).then((result) => {
          expect(result.kind).toBe('err');
          if (result.kind === 'err') {
            expect(result.error.stage).toBe('layout');
          }
        });
            })
        );
    });

    test.skip('render_failure', () => {
        // test generation failed: generated body does not reference the bound value `value`
    });

});

