// SPDX-License-Identifier: AGPL-3.0-or-later
// @generated by archebuild — do not edit. Regenerated each build.

import { Stylesheet } from '@archeglyph/proto/gen/style_pb';
import { Vec2, Vec2Schema } from '@archeglyph/proto/gen/style_pb';
import { create } from '@bufbuild/protobuf';

/**
 * Content assigned to an annotation before the user replaces it.
 *
 * An annotation's bounds are derived from its label, so an empty label would
 * make a newly-created annotation impossible to see or select.  This is
 * deliberately real content rather than a visual placeholder: it gives the
 * annotation a measurable label during creation and remains recoverable if
 * the user leaves it untouched.
 */
export const NEW_ANNOTATION_TEXT: string = 'New annotation';

/**
 * The box a new annotation occupies before the scene has measured its
 * label, in diagram units. 160 wide by 40 high.
 *
 * It is not written into the stylesheet and it is not the annotation's
 * real size -- layout derives that from the text. It exists only so the
 * inline editor has somewhere to render during the one scene rebuild
 * between creating an annotation and its bounds existing.
 */
export const NEW_ANNOTATION_SIZE: Vec2 = create(Vec2Schema, { x: 160, y: 40 });

export function newAnnotationId(stylesheet: Stylesheet, base: string): string {
  const annotations = stylesheet.annotations;
  if (!Object.prototype.hasOwnProperty.call(annotations, base)) {
    return base;
  }

  for (let suffix = 2; ; suffix += 1) {
    const candidate = `${base}-${suffix}`;
    if (!Object.prototype.hasOwnProperty.call(annotations, candidate)) {
      return candidate;
    }
  }
}
