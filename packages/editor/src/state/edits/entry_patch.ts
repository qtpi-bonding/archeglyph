// SPDX-License-Identifier: AGPL-3.0-or-later
// @generated by archebuild — do not edit. Regenerated each build.

import { create } from '@bufbuild/protobuf';
import {
  AnnotationAnchor,
  AnnotationEntry,
  AnnotationEntrySchema,
  AnnotationLayoutSchema,
  EdgeLayoutSchema,
  EdgeRouting,
  EdgeStyleEntry,
  EdgeStyleEntrySchema,
  Glyph1D,
  Glyph2D,
  GroupLabelPosition,
  GroupLayoutSchema,
  GroupRenderMode,
  GroupStyleEntry,
  GroupStyleEntrySchema,
  NodeLayoutSchema,
  NodeStyleEntry,
  NodeStyleEntrySchema,
  NodeVisibility,
  Typography,
  Vec2,
} from '@archeglyph/proto/gen/style_pb';
import { Localization } from '@archeglyph/proto/gen/content_pb';

export interface AnnotationEntryPatch {
  id: string;
  content?: Array<Localization>;
  anchor?: AnnotationAnchor;
  position?: Vec2;
  size?: Vec2;
  rotation?: number;
  shape?: Glyph2D;
  typography?: Typography;
  callout?: Glyph1D;
  component?: string;
}

export interface GroupEntryPatch {
  position?: Vec2;
  size?: Vec2;
  padding?: number;
  renderMode?: GroupRenderMode;
  labelPosition?: GroupLabelPosition;
  shape?: Glyph2D;
  typography?: Typography;
  component?: string;
}

export interface EdgeEntryPatch {
  routing?: EdgeRouting;
  waypoints?: Array<Vec2>;
  connection?: Glyph1D;
  typography?: Typography;
  component?: string;
}

export interface NodeEntryPatch {
  position?: Vec2;
  size?: Vec2;
  rotation?: number;
  shape?: Glyph2D;
  typography?: Typography;
  component?: string;
  visibility?: NodeVisibility;
}

export function patchNodeEntry(existing: NodeStyleEntry | undefined, patch: NodeEntryPatch): NodeStyleEntry {
  const hasLayoutPatch: boolean = patch.position !== undefined || patch.size !== undefined || patch.rotation !== undefined;
  return create(NodeStyleEntrySchema, {
    ...(existing ?? {}),
    ...(hasLayoutPatch ? {
      layout: create(NodeLayoutSchema, {
        ...(existing?.layout ?? {}),
        ...(patch.position === undefined ? {} : { position: patch.position }),
        ...(patch.size === undefined ? {} : { size: patch.size }),
        ...(patch.rotation === undefined ? {} : { rotation: patch.rotation }),
      }),
    } : {}),
    ...(patch.shape === undefined ? {} : { shape: patch.shape }),
    ...(patch.typography === undefined ? {} : { typography: patch.typography }),
    ...(patch.component === undefined ? {} : { component: patch.component }),
    ...(patch.visibility === undefined ? {} : { visibility: patch.visibility }),
  });
}

export function patchAnnotationEntry(existing: AnnotationEntry | undefined, patch: AnnotationEntryPatch): AnnotationEntry {
  const hasLayoutPatch: boolean = patch.position !== undefined || patch.size !== undefined || patch.rotation !== undefined;
  return create(AnnotationEntrySchema, {
    ...(existing ?? {}),
    id: patch.id,
    ...(patch.content === undefined ? {} : { content: patch.content }),
    ...(patch.anchor === undefined ? {} : { anchor: patch.anchor }),
    ...(hasLayoutPatch ? {
      layout: create(AnnotationLayoutSchema, {
        ...(existing?.layout ?? {}),
        ...(patch.position === undefined ? {} : { position: patch.position }),
        ...(patch.size === undefined ? {} : { size: patch.size }),
        ...(patch.rotation === undefined ? {} : { rotation: patch.rotation }),
      }),
    } : {}),
    ...(patch.shape === undefined ? {} : { shape: patch.shape }),
    ...(patch.typography === undefined ? {} : { typography: patch.typography }),
    ...(patch.callout === undefined ? {} : { callout: patch.callout }),
    ...(patch.component === undefined ? {} : { component: patch.component }),
  });
}

export function patchEdgeEntry(existing: EdgeStyleEntry | undefined, patch: EdgeEntryPatch): EdgeStyleEntry {
  const hasLayoutPatch: boolean = patch.routing !== undefined || patch.waypoints !== undefined;
  return create(EdgeStyleEntrySchema, {
    ...(existing ?? {}),
    ...(hasLayoutPatch ? {
      layout: create(EdgeLayoutSchema, {
        ...(existing?.layout ?? {}),
        ...(patch.routing === undefined ? {} : { routing: patch.routing }),
        ...(patch.waypoints === undefined ? {} : { waypoints: patch.waypoints }),
      }),
    } : {}),
    ...(patch.connection === undefined ? {} : { connection: patch.connection }),
    ...(patch.typography === undefined ? {} : { typography: patch.typography }),
    ...(patch.component === undefined ? {} : { component: patch.component }),
  });
}

export function patchGroupEntry(existing: GroupStyleEntry | undefined, patch: GroupEntryPatch): GroupStyleEntry {
  const hasLayoutPatch: boolean = patch.position !== undefined || patch.size !== undefined || patch.padding !== undefined || patch.renderMode !== undefined || patch.labelPosition !== undefined;
  return create(GroupStyleEntrySchema, {
    ...(existing ?? {}),
    ...(hasLayoutPatch ? {
      layout: create(GroupLayoutSchema, {
        ...(existing?.layout ?? {}),
        ...(patch.position === undefined ? {} : { position: patch.position }),
        ...(patch.size === undefined ? {} : { size: patch.size }),
        ...(patch.padding === undefined ? {} : { padding: patch.padding }),
        ...(patch.renderMode === undefined ? {} : { renderMode: patch.renderMode }),
        ...(patch.labelPosition === undefined ? {} : { labelPosition: patch.labelPosition }),
      }),
    } : {}),
    ...(patch.shape === undefined ? {} : { shape: patch.shape }),
    ...(patch.typography === undefined ? {} : { typography: patch.typography }),
    ...(patch.component === undefined ? {} : { component: patch.component }),
  });
}
