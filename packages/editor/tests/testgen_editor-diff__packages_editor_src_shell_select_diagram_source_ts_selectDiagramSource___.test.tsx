// Generated by `archegraph testgen` — do not edit by hand.
// Re-run `archegraph testgen --spec <name>` to regenerate.

import { describe, expect, test } from 'bun:test';
import { create } from '@bufbuild/protobuf';
import * as fc from 'fast-check';

import { UrlParamDiagramSource } from '../src/diff/url_param_diagram_source';
import { selectDiagramSource } from '../src/shell/select_diagram_source';

describe('testgen_shell__selectDiagramSource', () => {
    // WHEN: None of base_d, base_fetch, base_gh, or base_gist is present; the function returns undefined.
    // THEN: It returns undefined when none of base_d, base_fetch, base_gh, or base_gist is present.
    test('no_source_parameters', () => {
        const params = new URLSearchParams();
        expect(selectDiagramSource(params)).toBeUndefined();
    });

    test('base_d_present', () => {
        fc.assert(
            fc.property(fc.string(), (value) => {
        const params = new URLSearchParams();
        params.set('base_d', value);
        expect(selectDiagramSource(params)).toBeInstanceOf(UrlParamDiagramSource);
            })
        );
    });

    test('base_fetch_present', () => {
        fc.assert(
            fc.property(fc.string(), (value) => {
        const params = new URLSearchParams();
        params.set('base_fetch', value);
        expect(selectDiagramSource(params)).toBeInstanceOf(UrlParamDiagramSource);
            })
        );
    });

    test('base_gh_present', () => {
        fc.assert(
            fc.property(fc.record({ gh: fc.string(), path: fc.option(fc.string(), { nil: undefined }), ref: fc.option(fc.string(), { nil: undefined }) }), (value) => {
        const params = new URLSearchParams();
        params.set('base_gh', value.gh);
        if (value.path !== undefined) {
          params.set('base_path', value.path);
        }
        if (value.ref !== undefined) {
          params.set('base_ref', value.ref);
        }
        expect(selectDiagramSource(params)).toBeInstanceOf(UrlParamDiagramSource);
            })
        );
    });

    test('base_gist_present', () => {
        fc.assert(
            fc.property(fc.string(), (value) => {
        const params = new URLSearchParams();
        params.set('base_gist', value);
        expect(selectDiagramSource(params)).toBeInstanceOf(UrlParamDiagramSource);
            })
        );
    });

    test('modifiers_without_github_source', () => {
        fc.assert(
            fc.property(fc.record({ path: fc.option(fc.string(), { nil: undefined }), ref: fc.option(fc.string(), { nil: undefined }) }), (value) => {
        const params = new URLSearchParams();
        if (value.path !== undefined) {
          params.set('base_path', value.path);
        }
        if (value.ref !== undefined) {
          params.set('base_ref', value.ref);
        }
        expect(selectDiagramSource(params)).toBeUndefined();
            })
        );
    });

});

