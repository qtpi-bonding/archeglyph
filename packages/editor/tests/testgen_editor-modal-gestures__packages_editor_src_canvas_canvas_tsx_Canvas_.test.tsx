// Generated by `archegraph testgen` — do not edit by hand.
// Re-run `archegraph testgen --spec <name>` to regenerate.

import { describe, expect, test } from 'bun:test';

import { Canvas } from '../src/canvas/canvas';

describe('testgen_canvas__Canvas', () => {
    // WHEN: No modal gesture is open, no local pointer gesture is active, and no local preview is in flight; the OverlayLayer preview is undefined and dimmedRefs is empty unless no preview exists to generate callout refs.
    // THEN: It passes an undefined preview to OverlayLayer and returns no dimmed refs unless an active preview supplies callout annotation refs.
    test('idle_without_gesture', () => {
        expect(typeof Canvas).toBe('function');
    });

    // WHEN: No modal gesture is open and a local pointer move gesture is active with a local preview; the local preview is passed through to OverlayLayer and the moving refs are dimmed.
    // THEN: It passes the local move preview through to OverlayLayer and dims all moving refs, plus any callout refs from that preview.
    test('local_pointer_move_preview', () => {
        expect(typeof Canvas).toBe('function');
    });

    // WHEN: No modal gesture is open and a local pointer resize gesture is active with a local preview; the local preview is passed through to OverlayLayer and the resized ref is dimmed.
    // THEN: It passes the local resize preview through to OverlayLayer and dims the resized ref, plus any callout refs from that preview.
    test('local_pointer_resize_preview', () => {
        expect(typeof Canvas).toBe('function');
    });

    // WHEN: A modal grab gesture is open, geometry is defined, and modalPreview returns a preview; that modal preview is passed to OverlayLayer and every ref in the modal gesture is included in dimmedRefs.
    // THEN: It passes the modal grab preview to OverlayLayer and dims every ref in the modal gesture.
    test('modal_grab_with_preview', () => {
        expect(typeof Canvas).toBe('function');
    });

    // WHEN: A modal resize gesture is open, geometry is defined, and modalPreview returns a preview; that modal preview is passed to OverlayLayer and only the first modal ref is included in dimmedRefs.
    // THEN: It passes the modal resize preview to OverlayLayer and dims only the modal gesture's first ref.
    test('modal_resize_with_preview', () => {
        expect(typeof Canvas).toBe('function');
    });

    // WHEN: A modal gesture is open while geometry() is undefined; the modal preview is undefined because the geometry guard applies to the modal branch, while dimmedRefs still uses the modal grab refs or first modal resize ref.
    // THEN: It passes an undefined preview while still dimming all modal grab refs or the first modal resize ref.
    test('modal_gesture_without_geometry', () => {
        expect(typeof Canvas).toBe('function');
    });

    // WHEN: A modal gesture is open, geometry is defined, and modalPreview returns undefined; OverlayLayer receives an undefined preview, while dimmedRefs still contains the modal grab refs or first modal resize ref.
    // THEN: It passes an undefined preview while still dimming all modal grab refs or the first modal resize ref.
    test('modal_preview_undefined', () => {
        expect(typeof Canvas).toBe('function');
    });

    // WHEN: A modal gesture is open at the same time that local gesture() or local preview() has a value; both memos use the modal gesture as the higher-precedence source, so local preview and local moving/resizing refs are ignored for these props.
    // THEN: It uses the modal gesture's preview and target refs for both props, ignoring local preview and local gesture state.
    test('modal_precedence_over_local_state', () => {
        expect(typeof Canvas).toBe('function');
    });

    // WHEN: A modal grab gesture produces a preview whose bounds yield callout previews; the modal target refs are retained and any corresponding annotation refs are appended without duplicates.
    // THEN: It retains every modal grab ref and appends each corresponding callout annotation ref once.
    test('modal_grab_callout_dimming', () => {
        expect(typeof Canvas).toBe('function');
    });

    // WHEN: A modal resize gesture produces a preview whose bounds yield callout previews; the single modal resize target ref is retained and any corresponding annotation refs are appended without duplicates.
    // THEN: It retains the single modal resize target ref and appends each corresponding callout annotation ref once.
    test('modal_resize_callout_dimming', () => {
        expect(typeof Canvas).toBe('function');
    });

    // WHEN: A pointer-down event occurs while a modal gesture is open; Canvas clears the modal gesture before routing the press, preventing the modal and pointer interaction from being live together, after which normal pointer-down behavior proceeds.
    // THEN: It clears the modal gesture before routing the pointer press, so the normal pointer interaction proceeds alone.
    test('pointer_down_clears_modal', () => {
        expect(typeof Canvas).toBe('function');
    });

    // WHEN: A pointer-down event occurs with no modal gesture open; no modal state needs clearing and the existing pointer interaction routing proceeds unchanged.
    // THEN: It leaves modal state unchanged and performs the existing pointer interaction routing.
    test('pointer_down_without_modal', () => {
        expect(typeof Canvas).toBe('function');
    });

    // WHEN: A pointer-down event is the second click on an annotation; after modal state is cleared, Canvas enters text editing for that annotation and does not start a pointer gesture.
    // THEN: It clears the modal gesture, selects the annotation, enters text editing, and starts no pointer gesture.
    test('double_click_annotation', () => {
        expect(typeof Canvas).toBe('function');
    });

    // WHEN: A modal enter-resize gesture is open; its single target ref is treated like the resize case for dimming, because enter-resize only opens for a single selected element.
    // THEN: It dims the modal enter-resize gesture's sole target ref as the resize case does.
    test('modal_enter_resize_target', () => {
        expect(typeof Canvas).toBe('function');
    });

});

