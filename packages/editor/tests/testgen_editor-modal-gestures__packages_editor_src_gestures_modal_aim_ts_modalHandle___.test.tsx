// Generated by `archegraph testgen` — do not edit by hand.
// Re-run `archegraph testgen --spec <name>` to regenerate.

import { describe, expect, test } from 'bun:test';
import * as fc from 'fast-check';

import { modalHandle } from '../src/gestures/modal_aim';

describe('testgen_gestures__modalHandle', () => {
    test('up_direction', () => {
        fc.assert(
            fc.property(fc.constantFrom('grab' as const, 'resize' as const), (value) => {
        const gesture = { kind: value, refs: [], direction: 'up' as const, digits: '0' };
        expect(modalHandle(gesture)).toBe('n');
            })
        );
    });

    test('down_direction', () => {
        fc.assert(
            fc.property(fc.constantFrom('grab' as const, 'resize' as const), (value) => {
        const gesture = { kind: value, refs: [], direction: 'down' as const, digits: '0' };
        expect(modalHandle(gesture)).toBe('s');
            })
        );
    });

    test('left_direction', () => {
        fc.assert(
            fc.property(fc.constantFrom('grab' as const, 'resize' as const), (value) => {
        const gesture = { kind: value, refs: [], direction: 'left' as const, digits: '0' };
        expect(modalHandle(gesture)).toBe('w');
            })
        );
    });

    test('right_direction', () => {
        fc.assert(
            fc.property(fc.constantFrom('grab' as const, 'resize' as const), (value) => {
        const gesture = { kind: value, refs: [], direction: 'right' as const, digits: '0' };
        expect(modalHandle(gesture)).toBe('e');
            })
        );
    });

    // WHEN: The gesture direction is undefined; the function returns undefined.
    // THEN: returns undefined when the direction is undefined
    test('undefined_direction', () => {
        const gesture = { kind: 'grab' as const, refs: [], digits: '0' };
        expect(modalHandle(gesture)).toBeUndefined();
    });

});

