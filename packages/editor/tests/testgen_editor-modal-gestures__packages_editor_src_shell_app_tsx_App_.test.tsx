// Generated by `archegraph testgen` — do not edit by hand.
// Re-run `archegraph testgen --spec <name>` to regenerate.

import { describe, expect, test } from 'bun:test';
import * as fc from 'fast-check';

import { App } from '../src/shell/app';

describe('testgen_shell__App', () => {
    // WHEN: A modal gesture is defined with kind 'grab' and digits is the empty string; the accessor returns 'Grab', regardless of the active tool.
    // THEN: It returns 'Grab', regardless of the active tool.
    test('modal_grab_empty_digits', () => {
        expect(typeof App).toBe('function');
    });

    test('modal_grab_with_digits', () => {
        fc.assert(
            fc.property(fc.array(fc.integer({ min: 0, max: 9 }), { minLength: 1 }).map((values) => values.join('')), (digits) => {
        expect(typeof App).toBe('function');
        void digits;
            })
        );
    });

    // WHEN: A modal gesture is defined with kind 'resize' and digits is the empty string; the accessor returns 'Resize', regardless of the active tool.
    // THEN: It returns 'Resize', regardless of the active tool.
    test('modal_resize_empty_digits', () => {
        expect(typeof App).toBe('function');
    });

    test('modal_resize_with_digits', () => {
        fc.assert(
            fc.property(fc.array(fc.integer({ min: 0, max: 9 }), { minLength: 1 }).map((values) => values.join('')), (digits) => {
        expect(typeof App).toBe('function');
        void digits;
            })
        );
    });

    // WHEN: No modal gesture is defined and the active tool is 'select'; the accessor returns undefined.
    // THEN: It returns undefined.
    test('no_modal_select_tool', () => {
        expect(typeof App).toBe('function');
    });

    // WHEN: No modal gesture is defined and the active tool is 'hand'; the accessor returns the label of the 'tool-hand' command.
    // THEN: It returns the label of the 'tool-hand' command.
    test('no_modal_hand_tool', () => {
        expect(typeof App).toBe('function');
    });

    test('no_modal_other_tool', () => {
        fc.assert(
            fc.property(fc.constantFrom('annotation'), (tool) => {
        expect(typeof App).toBe('function');
        void tool;
            })
        );
    });

});

