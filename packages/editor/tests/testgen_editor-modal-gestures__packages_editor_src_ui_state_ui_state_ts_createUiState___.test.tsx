// Generated by `archegraph testgen` — do not edit by hand.
// Re-run `archegraph testgen --spec <name>` to regenerate.

import { describe, expect, test } from 'bun:test';

import { createUiState } from '../src/ui_state/ui_state';

describe('testgen_ui_state__createUiState', () => {
    // WHEN: The function is called with no arguments; the returned handle includes modalGesture and setModalGesture, with modalGesture initially undefined.
    // THEN: returns a handle containing modalGesture and setModalGesture, with modalGesture initially undefined.
    test('no_arguments', () => {
        const state = createUiState();
        expect(Object.keys(state)).toEqual(expect.arrayContaining(["modalGesture", "setModalGesture"]));
        expect(state).toHaveProperty("modalGesture", undefined);
        expect(state).toMatchObject({ setModalGesture: expect.any(Function) });
    });

});

