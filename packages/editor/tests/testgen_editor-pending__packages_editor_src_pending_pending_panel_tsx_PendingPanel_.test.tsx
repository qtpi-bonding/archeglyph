// Generated by `archegraph testgen` — do not edit by hand.
// Re-run `archegraph testgen --spec <name>` to regenerate.

import { describe, expect, test } from 'bun:test';
import * as fc from 'fast-check';

import { PendingPanel } from '../src/pending/pending_panel';

describe('testgen_pending__PendingPanel', () => {
    test('non_empty_items', () => {
        fc.assert(
            fc.property(fc.array(fc.string(), { minLength: 1 }), (value) => {
        void value;
        expect(typeof PendingPanel).toBe('function');
            })
        );
    });

    test('empty_items', () => {
        fc.assert(
            fc.property(fc.constant([]), (value) => {
        void value;
        expect(typeof PendingPanel).toBe('function');
            })
        );
    });

    test('selected_row', () => {
        fc.assert(
            fc.property(fc.array(fc.string(), { minLength: 1 }).map((ids: string[]) => ({ ids, selectedId: ids[0] })), (value) => {
        void value;
        expect(typeof PendingPanel).toBe('function');
            })
        );
    });

    test('unselected_or_missing_row', () => {
        fc.assert(
            fc.property(fc.oneof(fc.constant(undefined), fc.string()), (value) => {
        void value;
        expect(typeof PendingPanel).toBe('function');
            })
        );
    });

    test('accept_action', () => {
        fc.assert(
            fc.property(fc.string(), (value) => {
        void value;
        expect(typeof PendingPanel).toBe('function');
            })
        );
    });

    test('reject_action', () => {
        fc.assert(
            fc.property(fc.string(), (value) => {
        void value;
        expect(typeof PendingPanel).toBe('function');
            })
        );
    });

    test('sync_error', () => {
        fc.assert(
            fc.property(fc.string(), (value) => {
        void value;
        expect(typeof PendingPanel).toBe('function');
            })
        );
    });

    // WHEN: syncError is undefined; no sync error is rendered.
    // THEN: Renders no sync error when syncError is undefined.
    test('no_sync_error', () => {
        expect(typeof PendingPanel).toBe('function');
    });

    test('many_items_with_open_thread', () => {
        fc.assert(
            fc.property(fc.array(fc.string(), { minLength: 20 }).map((ids: string[]) => ({ ids, selectedId: ids[0] })), (value) => {
        void value;
        expect(typeof PendingPanel).toBe('function');
            })
        );
    });

});

