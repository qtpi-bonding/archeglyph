// Generated by `archegraph testgen` — do not edit by hand.
// Re-run `archegraph testgen --spec <name>` to regenerate.

import { describe, expect, test } from 'bun:test';
import * as fc from 'fast-check';

import { ThreadView } from '../src/pending/thread_view';

describe('testgen_pending__ThreadView', () => {
    test('non_empty_comments_in_order', () => {
        fc.assert(
            fc.property(fc.array(fc.anything(), { minLength: 1 }), (value) => {
        void value;
        expect(typeof ThreadView).toBe('function');
            })
        );
    });

    test('missing_or_empty_author', () => {
        fc.assert(
            fc.property(fc.constantFrom(undefined, ''), (value) => {
        void value;
        expect(typeof ThreadView).toBe('function');
            })
        );
    });

    test('markdown_body_as_plain_text', () => {
        fc.assert(
            fc.property(fc.oneof(fc.constant('**bold**'), fc.constant('_italic_'), fc.constant('[link](https://example.com)'), fc.constant('`code`'), fc.string()), (value) => {
        void value;
        expect(typeof ThreadView).toBe('function');
            })
        );
    });

    test('reply_to_ignored', () => {
        fc.assert(
            fc.property(fc.oneof(fc.constant(null), fc.constant(undefined), fc.string(), fc.integer()), (value) => {
        void value;
        expect(typeof ThreadView).toBe('function');
            })
        );
    });

    // WHEN: An empty comments list displays the literal text 'No comments yet' above the reply box.
    // THEN: Displays 'No comments yet' above the reply box when comments is empty.
    test('empty_comments', () => {
        expect(typeof ThreadView).toBe('function');
    });

    test('blank_reply_disabled', () => {
        fc.assert(
            fc.property(fc.constantFrom('', ' ', '  ', '\t', '\n', ' \t\n '), (value) => {
        void value;
        expect(typeof ThreadView).toBe('function');
            })
        );
    });

    test('nonblank_reply_submitted', () => {
        fc.assert(
            fc.property(fc.string().filter((text: string): boolean => text.trim().length > 0), (value) => {
        void value;
        expect(typeof ThreadView).toBe('function');
            })
        );
    });

    test('textarea_keydown_stopped', () => {
        fc.assert(
            fc.property(fc.string(), (value) => {
        void value;
        expect(typeof ThreadView).toBe('function');
            })
        );
    });

});

