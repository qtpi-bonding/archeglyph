// Generated by `archegraph testgen` — do not edit by hand.
// Re-run `archegraph testgen --spec <name>` to regenerate.

import { describe, expect, test } from 'bun:test';

import { ZOOM_STEP } from '../src/gestures/commands';

describe('testgen_gestures__ZOOM_STEP', () => {
    // WHEN: The constant has the declared numeric value 1.2 and serves as the multiplicative factor for one zoom-in press.
    // THEN: Defines ZOOM_STEP as the numeric multiplier 1.2 for one zoom-in press.
    test('declared_zoom_step', () => {
        expect(typeof ZOOM_STEP).toBe('number');
        expect(ZOOM_STEP).toBe(1.2);
    });

});

