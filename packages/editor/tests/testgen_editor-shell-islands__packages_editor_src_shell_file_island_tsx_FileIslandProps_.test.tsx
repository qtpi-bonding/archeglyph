// Generated by `archegraph testgen` — do not edit by hand.
// Re-run `archegraph testgen --spec <name>` to regenerate.

import { describe, expect, test } from 'bun:test';
import * as fc from 'fast-check';

import { FileIslandProps } from '../src/shell/file_island';

describe('testgen_shell__FileIslandProps', () => {
    test('normal_saved_document', () => {
        fc.assert(
            fc.property(fc.tuple(fc.string({ minLength: 1 }), fc.string({ minLength: 1 }), fc.func(fc.constant(undefined)), fc.func(fc.constant(undefined))), (value) => {
        const [fileName, theme, onSave, onEditorTheme] = value;
        const props = new FileIslandProps();
        const status = 'saved' as any;
        props.fileName = fileName;
        props.dirty = false;
        props.canSave = true;
        props.status = status;
        props.editorTheme = theme;
        props.onSave = onSave;
        props.onEditorTheme = onEditorTheme;
        expect(props.fileName).toBe(fileName);
        expect(props.status).toBe(status);
        expect(props.onSave).toBe(onSave);
        expect(props.onEditorTheme).toBe(onEditorTheme);
            })
        );
    });

    test('dirty_saveable_document', () => {
        fc.assert(
            fc.property(fc.tuple(fc.string({ minLength: 1 }), fc.string({ minLength: 1 }).map(value => value as any)), (value) => {
        const [fileName, status] = value;
        const props = new FileIslandProps();
        let saveCalls = 0;
        const onSave = () => { saveCalls += 1; };
        props.fileName = fileName;
        props.dirty = true;
        props.canSave = true;
        props.status = status;
        props.onSave = onSave;
        props.onEditorTheme = () => {};
        expect(props.dirty).toBe(true);
        expect(props.canSave).toBe(true);
        props.onSave();
        expect(saveCalls).toBe(1);
            })
        );
    });

    test('dirty_unsaveable_document', () => {
        fc.assert(
            fc.property(fc.tuple(fc.string(), fc.boolean()), (value) => {
        const [fileName, hasStatus] = value;
        const props = new FileIslandProps();
        props.fileName = fileName;
        props.dirty = true;
        props.canSave = false;
        props.status = hasStatus ? ('saved' as any) : undefined;
        props.onSave = () => {};
        props.onEditorTheme = () => {};
        expect(props.dirty).toBe(true);
        expect(props.canSave).toBe(false);
        expect(props.onSave).toBeDefined();
            })
        );
    });

    test('clean_unsaveable_document', () => {
        fc.assert(
            fc.property(fc.tuple(fc.string(), fc.boolean()), (value) => {
        const [fileName, hasStatus] = value;
        const props = new FileIslandProps();
        props.fileName = fileName;
        props.dirty = false;
        props.canSave = false;
        props.status = hasStatus ? ('saved' as any) : undefined;
        props.onSave = () => {};
        props.onEditorTheme = () => {};
        expect(props.dirty).toBe(false);
        expect(props.canSave).toBe(false);
            })
        );
    });

    test('no_save_controller', () => {
        fc.assert(
            fc.property(fc.string(), (value) => {
        const props = new FileIslandProps();
        props.fileName = value;
        props.dirty = false;
        props.canSave = false;
        props.status = undefined;
        props.onSave = () => {};
        props.editorTheme = 'default';
        props.onEditorTheme = () => {};
        expect(props.fileName).toBe(value);
        expect(props.status).toBeUndefined();
            })
        );
    });

    test('saving_status', () => {
        fc.assert(
            fc.property(fc.string(), (value) => {
        const props = new FileIslandProps();
        const status = 'saving' as any;
        props.fileName = value;
        props.dirty = false;
        props.canSave = true;
        props.status = status;
        props.onSave = () => {};
        props.editorTheme = 'default';
        props.onEditorTheme = () => {};
        expect(props.status).toBe(status);
        expect(props.fileName).toBe(value);
            })
        );
    });

    test('save_failure_status', () => {
        fc.assert(
            fc.property(fc.tuple(fc.string(), fc.string()), (value) => {
        const [fileName, errorMessage] = value;
        const props = new FileIslandProps();
        const status = 'error' as any;
        props.fileName = fileName;
        props.dirty = true;
        props.canSave = true;
        props.status = status;
        props.errorMessage = errorMessage;
        props.onSave = () => {};
        props.editorTheme = 'default';
        props.onEditorTheme = () => {};
        expect(props.status).toBe(status);
        expect(props.errorMessage).toBe(errorMessage);
            })
        );
    });

    test('error_message_present', () => {
        fc.assert(
            fc.property(fc.string({ minLength: 1 }), (value) => {
        const props = new FileIslandProps();
        props.fileName = 'document';
        props.dirty = true;
        props.canSave = true;
        props.status = 'error' as any;
        props.errorMessage = value;
        props.onSave = () => {};
        props.editorTheme = 'default';
        props.onEditorTheme = () => {};
        expect(props.errorMessage).toBe(value);
            })
        );
    });

    // WHEN: errorMessage is omitted or undefined, so no explicit error detail is available.
    // THEN: It provides no explicit error detail because errorMessage is undefined or omitted.
    test('error_message_absent', () => {
        const props = new FileIslandProps();
        props.fileName = 'document';
        props.dirty = false;
        props.canSave = false;
        props.status = undefined;
        props.onSave = () => {};
        props.editorTheme = 'default';
        props.onEditorTheme = () => {};
        expect(props.errorMessage).toBeUndefined();
    });

    // WHEN: fileName is the empty string, the minimum-length string boundary for the required name prop.
    // THEN: It accepts and displays the empty file name without inventing a name.
    test('empty_file_name', () => {
        const props = new FileIslandProps();
        props.fileName = '';
        props.dirty = false;
        props.canSave = false;
        props.status = undefined;
        props.onSave = () => {};
        props.editorTheme = 'default';
        props.onEditorTheme = () => {};
        expect(props.fileName).toBe('');
    });

    test('whitespace_file_name', () => {
        fc.assert(
            fc.property(fc.array(fc.constantFrom(' ', '\t', '\n', '\r'), { minLength: 1 }).map(chars => chars.join('')), (value) => {
        const props = new FileIslandProps();
        props.fileName = value;
        props.dirty = false;
        props.canSave = false;
        props.status = undefined;
        props.onSave = () => {};
        props.editorTheme = 'default';
        props.onEditorTheme = () => {};
        expect(props.fileName).toBe(value);
        expect(props.fileName.trim()).toBe('');
            })
        );
    });

    // WHEN: editorTheme is the empty string, the minimum-length string boundary for the required theme prop.
    // THEN: It accepts the empty editor theme value and passes theme changes through the callback when requested.
    test('empty_editor_theme', () => {
        const props = new FileIslandProps();
        let receivedTheme: string | undefined;
        props.fileName = 'document';
        props.dirty = false;
        props.canSave = false;
        props.status = undefined;
        props.editorTheme = '';
        props.onSave = () => {};
        props.onEditorTheme = (theme: string) => { receivedTheme = theme; };
        props.onEditorTheme('next-theme');
        expect(props.editorTheme).toBe('');
        expect(receivedTheme).toBe('next-theme');
    });

    test('theme_change_callback', () => {
        fc.assert(
            fc.property(fc.string(), (value) => {
        const props = new FileIslandProps();
        let receivedTheme: string | undefined;
        props.fileName = 'document';
        props.dirty = false;
        props.canSave = false;
        props.status = undefined;
        props.editorTheme = 'default';
        props.onSave = () => {};
        props.onEditorTheme = (theme: string) => { receivedTheme = theme; };
        props.onEditorTheme(value);
        expect(receivedTheme).toBe(value);
            })
        );
    });

    test('save_callback', () => {
        fc.assert(
            fc.property(fc.func(fc.constant(undefined)), (value) => {
        const props = new FileIslandProps();
        let saveCalls = 0;
        const onSave = () => { saveCalls += 1; };
        props.fileName = 'document';
        props.dirty = true;
        props.canSave = true;
        props.status = 'saved' as any;
        props.onSave = onSave;
        props.editorTheme = 'default';
        props.onEditorTheme = () => {};
        props.onSave();
        expect(saveCalls).toBe(1);
            })
        );
    });

    // WHEN: canSave is explicitly false, indicating that the save action is unavailable.
    // THEN: It treats saving as unavailable and does not offer the save action.
    test('false_save_capability', () => {
        const props = new FileIslandProps();
        props.fileName = 'document';
        props.dirty = false;
        props.canSave = false;
        props.status = undefined;
        props.onSave = () => {};
        props.editorTheme = 'default';
        props.onEditorTheme = () => {};
        expect(props.canSave).toBe(false);
    });

    // WHEN: canSave is explicitly true, indicating that the save action is available through onSave.
    // THEN: It treats saving as available through onSave and can offer the save action when appropriate.
    test('true_save_capability', () => {
        const props = new FileIslandProps();
        let saveCalls = 0;
        props.fileName = 'document';
        props.dirty = false;
        props.canSave = true;
        props.status = 'saved' as any;
        props.onSave = () => { saveCalls += 1; };
        props.editorTheme = 'default';
        props.onEditorTheme = () => {};
        expect(props.canSave).toBe(true);
        props.onSave();
        expect(saveCalls).toBe(1);
    });

    // WHEN: dirty is explicitly false, indicating that there are no unsaved changes.
    // THEN: It treats the document as having no unsaved changes.
    test('clean_state', () => {
        const props = new FileIslandProps();
        props.fileName = 'document';
        props.dirty = false;
        props.canSave = true;
        props.status = 'saved' as any;
        props.onSave = () => {};
        props.editorTheme = 'default';
        props.onEditorTheme = () => {};
        expect(props.dirty).toBe(false);
    });

    // WHEN: dirty is explicitly true, indicating that there are unsaved changes.
    // THEN: It treats the document as having unsaved changes.
    test('dirty_state', () => {
        const props = new FileIslandProps();
        props.fileName = 'document';
        props.dirty = true;
        props.canSave = true;
        props.status = 'saved' as any;
        props.onSave = () => {};
        props.editorTheme = 'default';
        props.onEditorTheme = () => {};
        expect(props.dirty).toBe(true);
    });

});

