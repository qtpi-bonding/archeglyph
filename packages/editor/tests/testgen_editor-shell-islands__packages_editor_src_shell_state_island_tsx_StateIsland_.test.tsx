// Generated by `archegraph testgen` — do not edit by hand.
// Re-run `archegraph testgen --spec <name>` to regenerate.

import { describe, expect, test } from 'bun:test';
import * as fc from 'fast-check';

import { StateIsland, StateIslandProps } from '../src/shell/state_island';

describe('testgen_shell__StateIsland', () => {
    // WHEN: Neither an error nor a mode is provided, so there is no state content to show.
    // THEN: Render no state content when neither an error nor a mode is provided.
    test('no_state', () => {
        expect(typeof StateIsland).toBe('function');
    });

    test('mode_only', () => {
        fc.assert(
            fc.property(fc.string({ minLength: 1 }), (value) => {
        expect(value.length).toBeGreaterThan(0);
        expect(typeof StateIsland).toBe('function');
            })
        );
    });

    test('error_without_dismiss', () => {
        fc.assert(
            fc.property(fc.string({ minLength: 1 }), (value) => {
        expect(value.length).toBeGreaterThan(0);
        expect(typeof StateIsland).toBe('function');
            })
        );
    });

    test('error_with_dismiss', () => {
        fc.assert(
            fc.property(fc.tuple(fc.string({ minLength: 1 }), fc.func(fc.constant(undefined))), (value) => {
        const [error, onDismiss] = value;
        expect(error.length).toBeGreaterThan(0);
        expect(typeof onDismiss).toBe('function');
        expect(typeof StateIsland).toBe('function');
            })
        );
    });

    test('error_precedes_mode', () => {
        fc.assert(
            fc.property(fc.tuple(fc.string({ minLength: 1 }), fc.string({ minLength: 1 })), (value) => {
        const [error, mode] = value;
        expect(error.length).toBeGreaterThan(0);
        expect(mode.length).toBeGreaterThan(0);
        expect(typeof StateIsland).toBe('function');
            })
        );
    });

    // WHEN: The error value is the empty string while a mode is provided; treat the empty error as absent and render the mode state rather than an empty error row.
    // THEN: Treat the empty error as absent and render the mode state.
    test('empty_error_with_mode', () => {
        expect(typeof StateIsland).toBe('function');
    });

    // WHEN: The mode value is the empty string and no error is provided; there is no non-empty mode label to render.
    // THEN: Render no mode row because the empty mode has no non-empty label.
    test('empty_mode', () => {
        expect(typeof StateIsland).toBe('function');
    });

    // WHEN: Both error and mode are explicitly empty strings; neither state has displayable content.
    // THEN: Render neither state because both values are empty strings.
    test('empty_state_values', () => {
        expect(typeof StateIsland).toBe('function');
    });

    test('long_error_wraps', () => {
        fc.assert(
            fc.property(fc.string({ minLength: 1000 }), (value) => {
        expect(value.length).toBeGreaterThanOrEqual(1000);
        expect(typeof StateIsland).toBe('function');
            })
        );
    });

    // WHEN: The error is the shortest non-empty message, consisting of one character; it is still rendered as an error and can include the dismiss control only when onDismiss is supplied.
    // THEN: Render the one-character error and include a dismiss control only when onDismiss is supplied.
    test('error_boundary_message', () => {
        expect(typeof StateIsland).toBe('function');
    });

    // WHEN: The mode is the shortest non-empty label, consisting of one character, with no error; render it as the sole mode row.
    // THEN: Render the one-character mode label as the sole mode row.
    test('mode_boundary_label', () => {
        expect(typeof StateIsland).toBe('function');
    });

    test('dismiss_without_error', () => {
        fc.assert(
            fc.property(fc.func(fc.constant(undefined)), (value) => {
        expect(typeof value).toBe('function');
        expect(typeof StateIsland).toBe('function');
            })
        );
    });

    test('root_class_stable', () => {
        fc.assert(
            fc.property(fc.oneof(fc.string({ minLength: 1 }), fc.string({ minLength: 1 })), (value) => {
        expect(value.length).toBeGreaterThan(0);
        expect(typeof StateIsland).toBe('function');
            })
        );
    });

});

