// Generated by `archegraph testgen` — do not edit by hand.
// Re-run `archegraph testgen --spec <name>` to regenerate.

import { describe, expect, test } from 'bun:test';
import * as fc from 'fast-check';

import { ZoomIslandProps } from '../src/shell/zoom_island';

describe('testgen_shell__ZoomIslandProps', () => {
    test('standard_positive_scale', () => {
        fc.assert(
            fc.property(fc.double({ min: Number.MIN_VALUE, max: Number.MAX_VALUE, noNaN: true }), (value) => {
        const calls: unknown[] = [];
        const props = new ZoomIslandProps();
        props.zoom = value;
        props.onCommand = (command: any) => {
          calls.push(command);
        };
        expect(props.zoom).toBe(value);
        expect(typeof props.onCommand).toBe('function');
        const command = {};
        props.onCommand(command as any);
        expect(calls).toEqual([command]);
            })
        );
    });

    // WHEN: zoom is exactly 1, representing 100% viewport scale, and onCommand is callable
    // THEN: Accepts 1 as 100% scale, formats it as the island's 100% display, and routes commands through onCommand.
    test('unity_scale', () => {
        const calls: unknown[] = [];
        const props = new ZoomIslandProps();
        props.zoom = 1;
        props.onCommand = (command: any) => {
          calls.push(command);
        };
        expect(props.zoom).toBe(1);
        expect(typeof props.onCommand).toBe('function');
        const command = {};
        props.onCommand(command as any);
        expect(calls).toEqual([command]);
    });

    test('fractional_positive_scale', () => {
        fc.assert(
            fc.property(fc.double({ min: Number.MIN_VALUE, max: 1, noNaN: true }).filter((value) => value > 0 && value < 1), (value) => {
        const calls: unknown[] = [];
        const props = new ZoomIslandProps();
        props.zoom = value;
        props.onCommand = (command: any) => {
          calls.push(command);
        };
        expect(props.zoom).toBe(value);
        expect(props.zoom).toBeGreaterThan(0);
        expect(props.zoom).toBeLessThan(1);
        expect(typeof props.onCommand).toBe('function');
        const command = {};
        props.onCommand(command as any);
        expect(calls).toEqual([command]);
            })
        );
    });

    test('greater_than_unity_scale', () => {
        fc.assert(
            fc.property(fc.double({ min: 1, max: Number.MAX_VALUE, noNaN: true }).filter((value) => value > 1), (value) => {
        const calls: unknown[] = [];
        const props = new ZoomIslandProps();
        props.zoom = value;
        props.onCommand = (command: any) => {
          calls.push(command);
        };
        expect(props.zoom).toBe(value);
        expect(props.zoom).toBeGreaterThan(1);
        expect(typeof props.onCommand).toBe('function');
        const command = {};
        props.onCommand(command as any);
        expect(calls).toEqual([command]);
            })
        );
    });

    // WHEN: zoom is exactly 0, the lower boundary of nonnegative numeric input; this is not a usable positive viewport scale
    // THEN: Rejects the zero scale as unusable and does not attempt viewport updates or command dispatch.
    test('zero_scale', () => {
        const calls: unknown[] = [];
        const props = new ZoomIslandProps();
        props.zoom = 0;
        props.onCommand = (command: any) => {
          calls.push(command);
        };
        expect(props.zoom).toBe(0);
        expect(typeof props.onCommand).toBe('function');
        expect(calls).toEqual([]);
    });

    test('negative_scale', () => {
        fc.assert(
            fc.property(fc.double({ min: -Number.MAX_VALUE, max: -Number.MIN_VALUE, noNaN: true }), (value) => {
        const calls: unknown[] = [];
        const props = new ZoomIslandProps();
        props.zoom = value;
        props.onCommand = (command: any) => {
          calls.push(command);
        };
        expect(props.zoom).toBe(value);
        expect(props.zoom).toBeLessThan(0);
        expect(typeof props.onCommand).toBe('function');
        expect(calls).toEqual([]);
            })
        );
    });

    test('non_finite_scale', () => {
        fc.assert(
            fc.property(fc.constantFrom(NaN, Infinity, -Infinity), (value) => {
        const calls: unknown[] = [];
        const props = new ZoomIslandProps();
        props.zoom = value;
        props.onCommand = (command: any) => {
          calls.push(command);
        };
        expect(props.zoom).toBe(value);
        expect(Number.isFinite(props.zoom)).toBe(false);
        expect(calls).toEqual([]);
            })
        );
    });

    test('missing_zoom', () => {
        fc.assert(
            fc.property(fc.boolean(), (value) => {
        const props = new ZoomIslandProps();
        if (value) {
          (props as any).zoom = undefined;
        }
        expect(props.zoom).toBeUndefined();
            })
        );
    });

    test('missing_command_dispatcher', () => {
        fc.assert(
            fc.property(fc.boolean(), (value) => {
        const props = new ZoomIslandProps();
        props.zoom = 1;
        if (value) {
          (props as any).onCommand = undefined;
        }
        expect(props.onCommand).toBeUndefined();
            })
        );
    });

});

