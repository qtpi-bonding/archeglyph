// Generated by `archegraph testgen` — do not edit by hand.
// Re-run `archegraph testgen --spec <name>` to regenerate.

import { describe, expect, test } from 'bun:test';
import * as fc from 'fast-check';

import { ZoomIsland, ZoomIslandProps } from '../src/shell/zoom_island';

describe('testgen_shell__ZoomIsland', () => {
    test('render_with_current_zoom', () => {
        fc.assert(
            fc.property(fc.double({ noNaN: true, noDefaultInfinity: true }), (value) => {
        const rendered = ZoomIsland({ zoom: value, onCommand: () => {} });
        expect(typeof ZoomIsland).toBe("function");
        expect(rendered).toBeDefined();
            })
        );
    });

    test('rounded_percentage_readout', () => {
        fc.assert(
            fc.property(fc.double({ noNaN: true, noDefaultInfinity: true }), (value) => {
        const rendered = ZoomIsland({ zoom: value, onCommand: () => {} });
        expect(typeof ZoomIsland).toBe("function");
        expect(rendered).toBeDefined();
            })
        );
    });

    test('pinch_crosses_digit_width', () => {
        fc.assert(
            fc.property(fc.constantFrom(0.9, 1, 1.1), (value) => {
        const rendered = ZoomIsland({ zoom: value, onCommand: () => {} });
        expect(typeof ZoomIsland).toBe("function");
        expect(rendered).toBeDefined();
            })
        );
    });

    test('readout_activation', () => {
        fc.assert(
            fc.property(fc.constantFrom("click", "Enter", " "), (value) => {
        const rendered = ZoomIsland({ zoom: 1, onCommand: () => {} });
        expect(typeof ZoomIsland).toBe("function");
        expect(rendered).toBeDefined();
            })
        );
    });

    test('minus_activation', () => {
        fc.assert(
            fc.property(fc.constantFrom("click", "Enter", " "), (value) => {
        const rendered = ZoomIsland({ zoom: 1, onCommand: () => {} });
        expect(typeof ZoomIsland).toBe("function");
        expect(rendered).toBeDefined();
            })
        );
    });

    test('plus_activation', () => {
        fc.assert(
            fc.property(fc.constantFrom("click", "Enter", " "), (value) => {
        const rendered = ZoomIsland({ zoom: 1, onCommand: () => {} });
        expect(typeof ZoomIsland).toBe("function");
        expect(rendered).toBeDefined();
            })
        );
    });

    test('fit_activation', () => {
        fc.assert(
            fc.property(fc.constantFrom("click", "Enter", " "), (value) => {
        const rendered = ZoomIsland({ zoom: 1, onCommand: () => {} });
        expect(typeof ZoomIsland).toBe("function");
        expect(rendered).toBeDefined();
            })
        );
    });

    // WHEN: The readout receives keyboard focus as a real button and consequently has a focus ring; the surrounding island's focus suppression does not remove this button behavior.
    // THEN: Makes the readout a keyboard-focusable real button with a focus ring despite the surrounding island's focus suppression.
    test('keyboard_focus_readout', () => {
        expect(typeof ZoomIsland).toBe("function");
        const rendered = ZoomIsland({ zoom: 1, onCommand: () => {} });
        expect(rendered).toBeDefined();
    });

});

