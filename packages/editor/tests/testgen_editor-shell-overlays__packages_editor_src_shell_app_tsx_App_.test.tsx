// Generated by `archegraph testgen` — do not edit by hand.
// Re-run `archegraph testgen --spec <name>` to regenerate.

import { describe, expect, test } from 'bun:test';
import * as fc from 'fast-check';

import { App } from '../src/shell/app';

describe('testgen_shell__App', () => {
    // WHEN: The editor state is null, so App renders the start screen instead of the editor shell.
    // THEN: It renders the start screen instead of the editor shell.
    test('no_editor_state', () => {
        expect(typeof App).toBe('function');
    });

    // WHEN: The editor state is non-null, so App renders the editor shell with IslandFrame and its canvas, toolbar, and inspector content.
    // THEN: It renders the editor shell with IslandFrame containing the canvas, toolbar, and inspector.
    test('editor_state_ready', () => {
        expect(typeof App).toBe('function');
    });

    // WHEN: The UI overlay signal is undefined, so neither overlay surface is mounted.
    // THEN: It mounts neither the palette nor the help overlay.
    test('no_overlay_open', () => {
        expect(typeof App).toBe('function');
    });

    // WHEN: The UI overlay identifies the palette, so the palette is mounted as a sibling of IslandFrame and its rows are built for that opening.
    // THEN: It mounts the palette as an IslandFrame sibling and builds its rows for that opening.
    test('palette_overlay_open', () => {
        expect(typeof App).toBe('function');
    });

    // WHEN: The UI overlay identifies help, so the help sheet is mounted as a sibling of IslandFrame using COMMANDS and KEYMAP directly.
    // THEN: It mounts the help sheet as an IslandFrame sibling using COMMANDS and KEYMAP directly.
    test('help_overlay_open', () => {
        expect(typeof App).toBe('function');
    });

    // WHEN: The palette is opened again after a previous opening, so its surface and query state start fresh rather than retaining the previous query.
    // THEN: It remounts the palette so its surface and query state start fresh.
    test('palette_reopened', () => {
        expect(typeof App).toBe('function');
    });

    // WHEN: The palette is open but no scene geometry has been published, so it still opens with command rows and has no element rows.
    // THEN: It opens the palette with command rows only and no element rows.
    test('palette_without_geometry', () => {
        expect(typeof App).toBe('function');
    });

    // WHEN: The palette is open and current scene geometry is available, so command rows are concatenated before element rows generated from the diagram geometry.
    // THEN: It concatenates command rows before element rows generated from the diagram geometry.
    test('palette_with_geometry', () => {
        expect(typeof App).toBe('function');
    });

    test('excluded_palette_commands', () => {
        fc.assert(
            fc.property(fc.constantFrom('open-palette', 'open-help', 'escape', 'nudge-up', 'nudge-down', 'nudge-left', 'nudge-right', 'ring-next', 'ring-prev'), (query) => {
        expect(typeof App).toBe('function');
            })
        );
    });

    // WHEN: The palette invokes onClose, which clears the overlay signal by setting it to undefined.
    // THEN: It clears the overlay signal by setting it to undefined.
    test('palette_close', () => {
        expect(typeof App).toBe('function');
    });

    test('choose_command_with_context', () => {
        fc.assert(
            fc.property(fc.constantFrom('undo', 'redo', 'delete', 'hide', 'tool-select', 'tool-hand', 'tool-annotation', 'zoom-in', 'zoom-out', 'zoom-reset', 'zoom-fit', 'add-annotation', 'edit-text', 'duplicate', 'select-all', 'focus-inspector', 'pin-all', 'unpin-all', 'auto-layout', 'reset-size', 'save'), (query) => {
        expect(typeof App).toBe('function');
            })
        );
    });

    test('choose_command_without_context', () => {
        fc.assert(
            fc.property(fc.constantFrom('undo', 'redo', 'delete', 'hide', 'tool-select', 'tool-hand', 'tool-annotation', 'zoom-in', 'zoom-out', 'zoom-reset', 'zoom-fit', 'add-annotation', 'edit-text', 'duplicate', 'select-all', 'focus-inspector', 'pin-all', 'unpin-all', 'auto-layout', 'reset-size', 'save'), (query) => {
        expect(typeof App).toBe('function');
            })
        );
    });

    test('choose_element_with_matching_entry', () => {
        fc.assert(
            fc.property(fc.constantFrom('node', 'edge', 'group', 'annotation'), (query) => {
        expect(typeof App).toBe('function');
            })
        );
    });

    test('choose_element_without_entry', () => {
        fc.assert(
            fc.property(fc.constantFrom('node', 'edge', 'group', 'annotation'), (query) => {
        expect(typeof App).toBe('function');
            })
        );
    });

    test('choose_element_without_context', () => {
        fc.assert(
            fc.property(fc.constantFrom('node', 'edge', 'group', 'annotation'), (query) => {
        expect(typeof App).toBe('function');
            })
        );
    });

    test('element_key_disambiguates_reference', () => {
        fc.assert(
            fc.property(fc.constantFrom('node', 'group', 'edge', 'annotation'), (query) => {
        expect(typeof App).toBe('function');
            })
        );
    });

    test('close_before_command', () => {
        fc.assert(
            fc.property(fc.constantFrom('undo', 'focus-inspector', 'save', 'tool-select', 'zoom-in'), (query) => {
        expect(typeof App).toBe('function');
            })
        );
    });

    // WHEN: A palette element action is chosen; clearing the overlay precedes selection and any viewport centering.
    // THEN: It clears the overlay before selecting the element and centering the viewport.
    test('close_before_element_action', () => {
        expect(typeof App).toBe('function');
    });

});

