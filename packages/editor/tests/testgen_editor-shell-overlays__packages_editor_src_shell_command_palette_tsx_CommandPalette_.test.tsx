// Generated by `archegraph testgen` — do not edit by hand.
// Re-run `archegraph testgen --spec <name>` to regenerate.

import { describe, expect, test } from 'bun:test';
import * as fc from 'fast-check';

import { CommandPalette } from '../src/shell/command_palette';

describe('testgen_shell__CommandPalette', () => {
    // WHEN: The palette is mounted by its caller while open; the text input receives focus immediately on mount.
    // THEN: Focuses the text input immediately when the palette mounts.
    test('mount_focuses_input', () => {
        expect(CommandPalette).toBeDefined();
    });

    test('query_produces_filtered_rows', () => {
        fc.assert(
            fc.property(fc.string(), (value) => {
        expect(CommandPalette).toBeDefined();
            })
        );
    });

    test('query_produces_no_rows', () => {
        fc.assert(
            fc.property(fc.string(), (value) => {
        expect(CommandPalette).toBeDefined();
            })
        );
    });

    test('query_change_resets_highlight', () => {
        fc.assert(
            fc.property(fc.tuple(fc.string(), fc.string()), (value) => {
        expect(CommandPalette).toBeDefined();
            })
        );
    });

    test('arrow_down_advances_highlight', () => {
        fc.assert(
            fc.property(fc.nat(), (value) => {
        expect(CommandPalette).toBeDefined();
            })
        );
    });

    test('arrow_down_clamps_at_last_row', () => {
        fc.assert(
            fc.property(fc.nat(), (value) => {
        expect(CommandPalette).toBeDefined();
            })
        );
    });

    test('arrow_up_retreats_highlight', () => {
        fc.assert(
            fc.property(fc.nat(), (value) => {
        expect(CommandPalette).toBeDefined();
            })
        );
    });

    test('arrow_up_clamps_at_first_row', () => {
        fc.assert(
            fc.property(fc.nat(), (value) => {
        expect(CommandPalette).toBeDefined();
            })
        );
    });

    test('enter_chooses_highlighted_row', () => {
        fc.assert(
            fc.property(fc.nat(), (value) => {
        expect(CommandPalette).toBeDefined();
            })
        );
    });

    // WHEN: Enter is pressed on the input while the filtered list is empty; no row action is chosen and no close callback is invoked.
    // THEN: Does nothing, including not choosing an action or calling onClose.
    test('enter_on_empty_list_does_nothing', () => {
        expect(CommandPalette).toBeDefined();
    });

    // WHEN: Escape is pressed on the input; onClose is called.
    // THEN: Calls onClose when Escape is pressed.
    test('escape_closes_palette', () => {
        expect(CommandPalette).toBeDefined();
    });

    test('row_click_chooses_row', () => {
        fc.assert(
            fc.property(fc.nat(), (value) => {
        expect(CommandPalette).toBeDefined();
            })
        );
    });

    // WHEN: The scrim itself is clicked; onClose is called.
    // THEN: Calls onClose when the scrim itself is clicked.
    test('scrim_click_closes_palette', () => {
        expect(CommandPalette).toBeDefined();
    });

    test('scrim_blocks_background_pointer', () => {
        fc.assert(
            fc.property(fc.nat(), (value) => {
        expect(CommandPalette).toBeDefined();
            })
        );
    });

    test('input_key_does_not_reach_global_handler', () => {
        fc.assert(
            fc.property(fc.string(), (value) => {
        expect(CommandPalette).toBeDefined();
            })
        );
    });

});

