// Generated by `archegraph testgen` — do not edit by hand.
// Re-run `archegraph testgen --spec <name>` to regenerate.

import { describe, expect, test } from 'bun:test';

import { KEYMAP } from '../src/ui_state/keymap';

describe('testgen_ui_state__KEYMAP', () => {
    // WHEN: The keymap contains an entry mapping the chord key 'k' with meta true to the 'open-palette' command.
    // THEN: The function should include a keymap entry mapping meta+K to the open-palette command.
    test('meta_k_opens_palette', () => {
        expect(KEYMAP).toContainEqual({ chord: { key: 'k', meta: true }, command: 'open-palette' });
    });

    // WHEN: The keymap contains an entry mapping the chord key '?' with shift true to the 'open-help' command; the shift modifier is required for the chord to match the US-layout question-mark key event.
    // THEN: The function should include a keymap entry mapping Shift+? to the open-help command, with shift required for matching the US-layout question-mark event.
    test('shift_question_opens_help', () => {
        expect(KEYMAP).toContainEqual({ chord: { key: '?', shift: true }, command: 'open-help' });
    });

});

