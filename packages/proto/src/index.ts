// SPDX-License-Identifier: AGPL-3.0-or-later
// Re-exports for generated proto types.
// Populated after `bun run proto:gen` produces files in ./gen/.
// Per docs/style-guide.md §2.6: explicit re-import + re-export only (no `export *`).
export {};
