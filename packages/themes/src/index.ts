// @generated by archebuild — do not edit. Regenerated each build.

import { create } from "@bufbuild/protobuf";
import { FontSpecSchema, ThemeSchema, TokensSchema, type Theme } from "@archeglyph/proto/gen/theme_pb";

export function darkTheme(): Theme {
  throw new Error('not implemented');
}
export function getBundledTheme(name: string): Theme {
  throw new Error('not implemented');
}
export function lightTheme(): Theme {
  return create(ThemeSchema, {
    name: 'light',
    tokens: create(TokensSchema, {
      colors: {
        background: '#fafafa',
        foreground: '#1a1a1a',
        muted: '#6b7280',
        primary: '#2563eb',
        accent: '#7c3aed',
        success: '#16a34a',
        warning: '#d97706',
        danger: '#dc2626',
      },
      fonts: {
        body: create(FontSpecSchema, { family: '-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif' }),
        mono: create(FontSpecSchema, { family: 'ui-monospace, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace' }),
      },
      sizes: {
        stroke_thin: 0.5,
        stroke_normal: 1.5,
        font_small: 11,
        font_normal: 13,
        font_large: 16,
      },
      spacings: {
        node_padding: 12,
        label_offset: 6,
        node_spacing: 24,
      },
      dashes: {
        dashed: '5,5',
        dotted: '2,3',
      },
      shapePaths: {},
    }),
  });
}
